You are adding the tiniest possible amount to an existing hand-drawn storyboard sketch.

Treat the supplied sketch as an almost locked drawing. It is NOT a request to redraw,
clean up, beautify, complete, or reinterpret the image.

YOUR ONLY JOB:
- Preserve every existing stroke, blank area, proportion, pose, camera angle, crop, and
  composition exactly as drawn.
- Add only 3–6 short, rough pencil strokes total, and only where they make an already
  implied element a little easier to read: a partial contour, an eyeline, a prop edge,
  a floor/contact line, or a tiny directional gesture.
- If no such addition is clearly useful, return the input effectively unchanged.

DO NOT:
- redraw any existing line; erase, smooth, straighten, or darken anything; change line weight;
  fill empty space; add shading, hatching, texture, colour, or polish;
- add a new object, character, facial feature, clothing detail, background element, text,
  border, caption, symbol, or visual effect;
- make an uncertain detail more specific than it already is.

The output must look like the director added a few tentative strokes during the same drawing
session, not like an AI made a new version. Keep the same rough black pencil-on-white-paper
look and return one 16:9 storyboard panel with no text.
